@model string[]
@{
    ViewBag.Title = "Error";
}

<div class="panel alert-danger">
    @foreach (var err in Model)
    {
    <div class="alert alert-warning mb-2" role="alert">
        <strong>@err</strong>
    </div>
    }
</div>


